# Ejemplo de archivo .env para configuración local
# Copia este archivo a `.env` y ajusta los valores según tu entorno local.

DB_HOST=127.0.0.1
DB_NAME=atlantisbd
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4

# Opcional: cambiar a 'local' para entornos de desarrollo
# APP_ENV=local

# Nota: no subas el archivo `.env` real a repositorios públicos.
